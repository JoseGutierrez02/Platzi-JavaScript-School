var firstName = 'Jose', lastName = 'Gutierrez';

var nameInUpperCase = firstName.toUpperCase();
var lastNameInLowerCase = lastName.toLocaleLowerCase();

var firstLetterOfName = firstName.charAt(0);
var LetterCountOfName = firstName.length;

var fullName = `${firstName} ${lastName.toUpperCase()}`;

var str = firstName.substr(1, 2);